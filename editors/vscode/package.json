{
  "name": "grepnavi-lsp",
  "displayName": "grepnavi",
  "description": "C/C++ navigation via grepnavi -lsp: definition, references, hover, call hierarchy, completion, outline",
  "version": "0.1.5",
  "publisher": "grepnavi-local",
  "license": "MIT",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "activationEvents": [
    "onLanguage:c",
    "onLanguage:cpp"
  ],
  "main": "./extension.js",
  "contributes": {
    "configuration": {
      "title": "grepnavi",
      "properties": {
        "grepnavi.serverPath": {
          "type": "string",
          "default": "grepnavi",
          "scope": "machine",
          "description": "grepnavi 実行ファイルのパス。-lsp 付きで起動される。PATH に無い場合はフルパスを指定する。machine スコープ: 開いたリポジトリの .vscode/settings.json からは変更できない（任意 exe を起動させないため）"
        },
        "grepnavi.defines": {
          "type": "string",
          "default": "",
          "scope": "resource",
          "description": "#ifdef の構成。\"CONFIG_X=1 DEBUG=0 NDEBUG\" のように空白区切りで書く（値を省くと 1）。偽になるブロックと #if 0 が薄く表示される。GUI の #ifdef 条件リストと同じ書式"
        },
        "grepnavi.statusBarLabel": {
          "type": [
            "string",
            "null"
          ],
          "default": null,
          "description": "ステータスバーの接続表示に出す文字。未設定なら拡張の表示名、空文字なら状態アイコンだけになる。項目ごと消したいときはステータスバーを右クリックして非表示にする"
        }
      }
    }
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  }
}
